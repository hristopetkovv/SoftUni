@model AnimalShop.Web.ViewModels.Products.ProductListingViewModel

@{
    this.ViewData["Title"] = "Fishes Aquariums";
}

<div>
    <p>
        <strong>@Model.Count product(s) found</strong>
    </p>
</div>

<div class="row">
    @foreach (var aquarium in Model.Products)
    {
        <partial name="../Shared/_ProductPartialView.cshtml" model="aquarium" />
    }
</div>
