@model AnimalShop.Web.ViewModels.Products.ProductListingViewModel

@{
    this.ViewData["Title"] = "Cats Bowls";
}

<div>
    <p>
        <strong>@Model.Count product(s) found</strong>
    </p>
</div>

<div class="row">
    @foreach (var bowl in Model.Products)
    {
        <partial name="../Shared/_ProductPartialView.cshtml" model="bowl" />
    }
</div>
